form:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.form
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=60
  volumes:
    - form_data:/var/lib/data
  restart: unless-stopped
