route:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.route
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=120
  volumes:
    - route_data:/var/lib/data
  restart: unless-stopped
