navbar:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.navbar
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=120
  volumes:
    - navbar_data:/var/lib/data
  restart: unless-stopped
